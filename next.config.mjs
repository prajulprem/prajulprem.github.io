/** @type {import('next').NextConfig} */
const nextConfig = {};
// next.config.js
module.exports = {
  output: 'export',
  // You may also need to set the basePath if your repo name is different
  basePath: '/prajulprem.github.io', // Replace with your GitHub repo name
  trailingSlash: true,
};

export default nextConfig;
